{
  "name": "mf",
  "private": true,
  "scripts": {
    "bootstrap": "lerna bootstrap",
    "dev": "lerna run --parallel serve",
    "build": "lerna run build",
    "lint": "lerna run lint"
  },
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "lerna": "^4.0.0"
  }
}
